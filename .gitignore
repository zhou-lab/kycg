# build products
*.o
*.a
/kycg

# test binaries (tests/*.c compile to extensionless binaries beside them).
# Listed by name rather than by pattern: tests/ also holds .c and .R sources
# that must stay tracked.
/tests/test_args
/tests/test_hypergeo

# local working notes, not part of the published repo
CLAUDE.md
TODO.md

# editor / OS
.DS_Store
*~
compile_commands.json
.cache/
/tests/test_enrich
